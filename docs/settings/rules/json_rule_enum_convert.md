# json.rule.enum.convert

Enum type conversion during JSON serialization.

## Usage

```properties
json.rule.enum.convert=groovy:it.type().isEnum() => java.lang.String
```

See also [enum rules](./enum_use_name) for more granular control over enum handling.
