# tool

General utility methods.

## Common Methods

- `tool.trim(str)` — Trim whitespace
- `tool.isEmpty(str)` — Check if string is empty
- `tool.isNotEmpty(str)` — Check if string is not empty
- `tool.firstLine(str)` — Get the first line of text
