# field.type

Set the field type in API documentation.

## Usage

```properties
field.type=groovy:it.type().name()
```

This rule allows you to customize how field types are displayed, for example converting internal types to standard types.
