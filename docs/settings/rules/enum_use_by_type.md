# enum.use.by.type

Determine enum representation based on the field type.

## Usage

```properties
enum.use.by.type=true
```

When enabled, enums used as Integer fields will use ordinal values, and enums used as String fields will use name values.
